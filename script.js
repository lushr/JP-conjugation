let allCards=[],deck=[],order=[],currentIndex=0;
const promptEl=document.getElementById("englishPrompt"),answerEl=document.getElementById("japaneseAnswer"),answerCard=document.getElementById("answerCard"),revealButton=document.getElementById("revealButton"),nextButton=document.getElementById("nextButton"),progressText=document.getElementById("progressText"),progressBar=document.getElementById("progressBar"),modeLabel=document.getElementById("modeLabel"),filterButtons=document.querySelectorAll(".level-button"),helpButton=document.getElementById("helpButton"),helpPanel=document.getElementById("helpPanel"),closeHelp=document.getElementById("closeHelp");
function shuffleOrder(){order=[...Array(deck.length).keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}currentIndex=0}
function applyFilter(filter){deck=filter==="all"?[...allCards]:allCards.filter(c=>c.type===filter);shuffleOrder();filterButtons.forEach(b=>b.classList.toggle("active",b.dataset.filter===filter));modeLabel.textContent={all:"ALL",verb:"VERBS","i-adj":"い-ADJ","na-adj":"な-ADJ"}[filter];showCard()}
function showCard(){if(!deck.length)return;if(currentIndex>=order.length)shuffleOrder();const c=deck[order[currentIndex]];promptEl.textContent=c.english;answerEl.textContent=c.japanese;answerCard.classList.add("hidden");revealButton.classList.remove("hidden");progressText.textContent=`${currentIndex+1}/${deck.length}`;progressBar.style.width=`${((currentIndex+1)/deck.length)*100}%`}
revealButton.addEventListener("click",()=>{answerCard.classList.remove("hidden");revealButton.classList.add("hidden")});nextButton.addEventListener("click",()=>{currentIndex++;showCard();window.scrollTo({top:0,behavior:"smooth"})});filterButtons.forEach(b=>b.addEventListener("click",()=>applyFilter(b.dataset.filter)));helpButton.addEventListener("click",()=>helpPanel.classList.remove("hidden"));closeHelp.addEventListener("click",()=>helpPanel.classList.add("hidden"));helpPanel.addEventListener("click",e=>{if(e.target===helpPanel)helpPanel.classList.add("hidden")});
async function loadDeck(){try{const r=await fetch("conjugations.json");if(!r.ok)throw new Error();allCards=await r.json();applyFilter("all")}catch(e){promptEl.textContent="Error loading conjugation deck";progressText.textContent="0/0"}}
if("serviceWorker"in navigator){window.addEventListener("load",()=>navigator.serviceWorker.register("service-worker.js").catch(()=>{}))}loadDeck();